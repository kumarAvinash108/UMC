node_modules/
dist/
build/
.expo/
# Expo-generated native projects only (NOTE: bare `android/` would also match
# apps/android/, hiding the whole Expo app from git — scope it instead).
apps/android/android/
apps/android/ios/
.expo-shared/
*.log
.ucm/
.env
.env.local
coverage/
sqlite/*.db
*.tgz
target/
